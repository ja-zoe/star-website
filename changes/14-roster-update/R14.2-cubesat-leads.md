# R14.2 — CubeSat subteam lead updates

**Status:** done
**Files:** `src/routes/cubesat/subteam-descriptions/FSW.tsx`, `src/routes/cubesat/subteam-descriptions/Payload.tsx`

## Spec

**Problem:** two CubeSat subteam modals credit outgoing leads.

**Approach:** each subteam file holds a local `Subteam` object consumed by
`SubteamModal`; change only the `lead` string:

- `FSW.tsx`: `lead: "Adil Hydari"` → `"Seth Caskey"`.
- `Payload.tsx`: `lead: "Daniel Jedrzejczyk"` → `"Christian Metchenko"`.

`leadLabel` stays `"Lead"` (single lead in both). No other subteam files change.

## Tests

Trivial content edit — merge gate for `feat/set14/R14.2-cubesat-leads`:

- [x] `pnpm build` passes.
- [x] Manual (Playwright vs `pnpm dev`, `/cubesat`): subteam cards and modals show
      "Lead · Seth Caskey" (Flight Software) and "Lead · Christian Metchenko" (Payload);
      no console errors.

## Notes / log
- 2026-07-02 — Implemented per spec (two one-line `lead` edits). Verified cards + modals
  in-browser via Playwright. Merged into feat/set14-roster-update.
