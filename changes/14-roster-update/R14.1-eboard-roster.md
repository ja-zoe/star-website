# R14.1 — E-board roster update (2026–27)

**Status:** planned
**Files:** `src/routes/home/EboardSection.tsx`; delete `public/eboard/{sandhya,shilpi,saarim}.webp`

## Spec

**Problem:** `EboardSection.tsx` shows the outgoing e-board. Three members left (Sandhya
Ganesh, Shilpi Shah, Saarim Syed), three joined without photos yet, and everyone else
changed position. The new roster (Discord usernames were provided for reference only —
**never render them**):

| Position | Name | Photo |
|---|---|---|
| President | Kanika Syal | `kanika.webp` (existing) |
| Vice President | Praneeth Sai Damarla | `praneeth.webp` (existing) |
| Treasurer | Sasho Petrov | none — icon placeholder |
| Outreach Coordinator | Nila Anbumani | `nila.webp` (existing) |
| Social Media Coordinator | Aayushi Mallik | `aayushi.webp` (existing) |
| Webmaster | Julian Vilfort | `julian.webp` (existing) |
| EGC Representative | Vanshika Gupta | none — icon placeholder |
| SEDS Representative | Venya Tiwari | none — icon placeholder |

**Approach:** all state is the local `eboard: EboardMember[]` array in
`EboardSection.tsx` — no data files, no fetch.

- Rewrite the array to the eight members above, **in the table's order** (hierarchy order
  as given by the user; the current order is arbitrary). Update `name`/`position`;
  Praneeth's display name becomes "Praneeth Sai Damarla" per the user's listing.
- Keep each returning member's existing `styleAdjustments` crop tweaks (julian
  `objectPosition: "65% 25px"`, praneeth `"75% 20px"`, kanika `"55%"`) — the photos are
  unchanged, only positions/order change. `major`/`hometown` for new members: leave `""`
  (the render doesn't display these fields today anyway).
- Make `picture` optional in `EboardMember` (`picture?: string`, and fix the `any` while
  there). In the render, when `picture` is absent, render a placeholder inside the same
  `rounded-full` red-bordered circle: lucide `UserRound` icon (CONTEXT: lucide-react
  only), sized to fill (~`w-28 h-28`), muted `text-white/40`, centered on a subtle
  `bg-white/5`; mark it `aria-hidden` and keep the member's name/position text as the
  accessible content (no `<img>`, so no alt needed).
- Remove the now-unused `sandhya`/`shilpi`/`saarim` imports and delete their webp files
  from `public/eboard/` (they're dead weight; git history retains them, and CONTEXT flags
  public-asset size as a standing concern).

## Tests

Merge gate for `feat/set14/R14.1-eboard-roster` → `feat/set14-roster-update`:

- [ ] `pnpm build` (tsc + vite) passes with the removed imports and optional `picture`.
- [ ] `pnpm lint` passes.
- [ ] Manual (`pnpm dev`, home page `#EboardSection`): eight cards in the order above;
      correct name/position pairs; no Discord usernames anywhere.
- [ ] Sasho, Vanshika, Venya show the person-icon circle (same red border/size as photo
      cards); Kanika/Praneeth/Julian photos still cropped sensibly.
- [ ] No broken-image icons or 404s in the console (deleted webps referenced nowhere).

## Notes / log
