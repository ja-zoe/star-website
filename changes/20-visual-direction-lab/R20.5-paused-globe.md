# R20.5 — Rutgers-facing paused globe

**Status:** tests passing
**Files:** `src/components/magicui/globe.tsx`

## Spec

**Problem:** Turning motion off destroys the COBE instance after its first frame. Because WebGL does
not preserve that drawing buffer, the globe disappears instead of becoming a useful static visual.

**Approach:** Keep the COBE instance mounted while motion is reduced and stop incrementing its rotation.
Reset to a deterministic paused rotation that keeps the New Brunswick marker in frame. Preserve the
existing offscreen/page-hidden lifecycle, pointer behavior when motion is enabled, and running-state data
attribute. The paused frame must remain visible for both the site control and system reduced motion.

## Tests

- [x] `pnpm lint` and `pnpm build` pass.
- [x] Playwright screenshot confirms the globe and New Brunswick marker remain visible after pausing.
- [x] Resuming motion rotates from the visible globe; pausing again returns to the Rutgers-facing frame.
- [x] System reduced motion renders the same static globe without console errors or overflow.

## Notes / log

- 2026-08-30 — Preserved the COBE drawing buffer, kept one instance across preference changes, and
  used its renderer toggle to suspend work after the static frame settles. Paused states reset to a
  Rutgers-facing phi and use a deterministic DOM marker at the matching projected New Brunswick
  position; rotating states retain COBE's native marker. User and system motion paths passed.
