# R20.6 — Interactive exploded CubeSat hero prototype

**Status:** done
**Files:** `src/routes/home/HeroSection.tsx`, `src/components/hero/ExplodedCubeSat.tsx`, design records

## Spec

**Problem:** The hero has a strong editorial layout but lacks a distinctive hardware visual. The right
column is useful navigation, yet it does not immediately communicate the physical systems students build.

**Approach:** Add a stylized parametric CubeSat assembly above the existing equal-prominence program links
in the hero's right column. Build it with the existing React Three Fiber/Three.js stack and lazy-load its
chunk after the initial interface is usable. Model a central bus, frame, avionics/payload faces, antenna,
and paired solar arrays with restrained STAR-red hardware accents. Start in an exploded view; provide a
clear assemble/explode control and pointer-drag rotation. Allow slow idle rotation only when motion is
enabled. Reduced motion keeps a useful static exploded frame and all controls keyboard accessible. Keep
the existing CTA-left/projects-right composition and do not replace or reprioritize the three program links.

## Tests

- [x] `pnpm lint` and `pnpm build` pass; Three.js remains in a separate lazy chunk.
- [x] Desktop and mobile screenshots show a legible hardware model without obscuring hero copy or links.
- [x] Pointer drag and assemble/explode interaction work; keyboard control exposes the same state change.
- [x] Site pause and system reduced motion retain a static exploded model without continuous rotation.
- [x] Hero CLS, horizontal overflow, console errors, and project-link accessibility remain clean.

## Notes / log

- 2026-08-30 — Added a parametric central bus/frame, payload optics, avionics board, antenna module,
  and cell-detailed solar arrays in a fixed-height instrument stage above the unchanged program links.
  The 7.7kB model component and shared 852.8kB React Three Fiber runtime are separately lazy-loaded
  after 350ms; the model request is absent immediately after navigation. Assemble/explode, keyboard,
  drag, pause, system reduced motion, and offscreen suspension passed. Desktop/mobile CLS and overflow
  measured 0 with three accessible project links retained.
- 2026-08-30 — Test-passing feature branch merged into the Set 20 branch.
