# R20.3 — Production detail refinements

**Status:** tests passing, awaiting review
**Files:** navigation, project cards and canvas reveal, Join CTA, About globe
caption, E-board portraits, partner rail/assets, global footer, design records

## Spec
- Keep the selected production composition and equal project hierarchy.
- Center the desktop navigation and Projects dropdown independently of the
  motion control.
- Restore the lazy-loaded dot-matrix CanvasRevealEffect to each project card's
  visual field without hiding permanent card content or loading Three.js on the
  initial route chunk.
- Use NoiseBackground around the Join Discord CTA instead of MovingBorderButton.
- Change the globe caption to “Located in New Brunswick, New Jersey.”
- Show E-board portraits in color at rest.
- Add the official United States Air Force symbol as the eighth network item and
  keep all network rows centered at every breakpoint.
- Make the global footer transparent so it shares the page-wide starfield.

## Tests
- [x] `pnpm lint` and `pnpm build` pass.
- [x] Desktop navigation and Projects dropdown are centered; mobile navigation remains usable.
- [x] Project cards remain equal-size and canvas reveal appears on pointer/focus without initial Three.js loading.
- [x] Join CTA uses the noise edge and respects reduced motion.
- [x] Globe caption, color portraits, eight centered network items, and starred footer render at desktop/mobile widths.
- [x] Home and project routes have no overflow, console errors, or CLS regression.

## Notes / log
- 2026-08-29 — Restored the prior dot-matrix shader on the selected equal-card layout using stable
  React Three Fiber 9.7.0. The 857 kB canvas chunk is lazy and absent before first interaction.
- Desktop navigation/dropdown centers measured at 720px in a 1440px viewport. Network renders 4×2
  on desktop and 2×4 at 390px. Project cards remain 350×352px on mobile.
- Reduced motion produces no card canvas and stops noise/stars. Home and Robotics measured CLS 0,
  overflow 0, no console errors, and starfield coverage through the transparent footer.
- Air Force/UNP relationship verified against primary UNP and AFRL sources; symbol provenance and
  trademark deployment note are recorded in `public/THIRD_PARTY_ASSETS.md`.
