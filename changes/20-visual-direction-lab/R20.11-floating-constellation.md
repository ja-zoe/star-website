# R20.11 — Floating constellation treatment

**Status:** tests passing
**Files:** `src/routes/designLab/heroVisuals/KineticStarConstellation.tsx`

## Spec

**Problem:** Option C is the selected direction, but its visible rectangular border and opaque
panel background make the constellation read as a contained widget rather than part of the hero.

**Approach:** Remove the constellation figure's border and panel fill in the isolated comparison
route. Preserve its dimensions, interaction field, canvas edge fade, labels, project signal states,
motion lifecycle, and accessibility. Keep Options A and B and the production homepage unchanged.

## Tests

- [x] `pnpm lint` and `pnpm build` pass.
- [x] At 1440x900 and 390x844, Option C has no computed border or opaque panel background and reads
  as a floating composition without clipping or horizontal overflow.
- [x] All three project controls still update the constellation and correct project accent by
  pointer, keyboard focus, and tap.
- [x] Reduced-motion output remains complete; viewport/document lifecycle, 30 fps cap, and 1.5 DPR
  cap remain intact.
- [x] The comparison route has zero console errors/warnings and the production homepage, Options A,
  and Option B are unchanged.

## Notes / log

- 2026-08-31 — Removed Option C's figure border, opaque fill, and clipping so its transparent canvas
  edge blends into the shared star field. The canvas dimensions, labels, controls, and rendering
  lifecycle were preserved.
- 2026-08-31 — Playwright verified transparent/frameless computed styles, zero overflow, and visual
  output at 1440x900 and 390x844. All nine pointer/focus/tap project states, correct accents,
  reduced-motion output, viewport/document pausing, canvas budgets, and a clean console passed.
  `pnpm lint`, `pnpm build`, and `git diff --check` also passed.
