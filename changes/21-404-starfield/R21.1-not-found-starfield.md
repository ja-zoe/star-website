# R21.1 — Not Found starfield

**Status:** done
**Files:** `src/routes/NotFound.tsx`

## Spec

**Problem:** The global `StarsBackground` and `ShootingStars` render on every route, but the Not Found
page's opaque black background covers them, making the 404 feel disconnected from the rest of STAR's
visual identity.

**Approach:** Remove only the Not Found page's opaque background utility so its existing elevated
content renders over the shared global atmosphere. Do not mount a second star canvas, change the
404 copy, or modify other routes. The existing global motion preference and visibility lifecycle
remain the single source of truth for the background animation.

## Tests

- [x] `pnpm lint` and `pnpm build` pass.
- [x] A direct unknown URL renders the 404 content over a transparent route layer with the global
  star canvas visible at 1440x900 and 390x844.
- [x] The 404 has no horizontal overflow, console errors/warnings, or CLS regression.
- [x] User and system reduced-motion preferences keep the stars visible but static.
- [x] The homepage retains its existing background and production constellation unchanged.

## Notes / log

- 2026-08-31 — Removed the Not Found page's opaque background so it inherits the existing global
  `StarsBackground` and `ShootingStars` layers without mounting duplicate effects.
- 2026-08-31 — Playwright verified the transparent route layer and painted shared canvas at
  1440x900 and 390x844, a single star canvas, zero overflow, zero CLS, and zero console errors or
  warnings. User/system reduced-motion states kept visible static stars, and the production homepage
  constellation remained present. `pnpm lint`, `pnpm build`, and `git diff --check` passed.
- 2026-08-31 — Merged `feat/set21/R21.1-not-found-starfield` into
  `feat/set21-404-starfield` after the feature test scheme passed.
