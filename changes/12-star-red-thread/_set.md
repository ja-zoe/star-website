# Revision Set 12 — STAR red brand thread on project pages

Bootstrap: read `changes/CONTEXT.md` + `design/tokens.md` (Project signature accents →
`--brand`) + `design/components.md` (Project page kit → STAR red brand thread). Branch
`feat/set12-star-red-thread` off main.

User request: the project pages lead with their per-project accent but don't carry STAR red, so
they don't feel tied to the brand. Weave STAR red `#9D2626` through the *shared* chrome of every
project page (alongside each page's accent) so the three read as one branded family. Design spec
updated first (design-brand re-resolve: red promoted to an explicit `--brand` thread for project
pages). Implement autonomously — no questions.

## Status
- [t] R12.1 — STAR red brand thread — hero eyebrow STAR mark + red strand in hero wave + "A STAR Project" brand tie

## Open questions / decisions
None — implement autonomously per the user's instruction. All design calls recorded in R12.1.

## DB changes in this set
None.

## Log
- 2026-06-29 — Set 12 scaffolded off main; design spec re-resolved (red brand thread).
